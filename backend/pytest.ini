[pytest]
# 从项目根目录（backend 的上一级）作为导入根，保证 `backend.app.*` 可被导入
pythonpath = ..
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q
filterwarnings =
    ignore::DeprecationWarning
